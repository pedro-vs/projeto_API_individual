apiVersion: v1
kind: Service
metadata:
  name: exchange-service
  namespace: store-platform
spec:
  selector:
    app: exchange-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
