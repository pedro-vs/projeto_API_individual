apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: store-platform
spec:
  selector:
    app: product-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
